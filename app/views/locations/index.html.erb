<p id="notice"><%= notice %></p>

<h2 class="center">Company Locations</h2>
<%= render partial: 'shared/locations',
            locals: { locations: @locations, paginate: true } %>
<div class="center">
  <%= link_to 'New Location', new_location_path, class: 'btn btn-default' %>
</div>
